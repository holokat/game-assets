import type { MoveId } from './types';

/** The default hold and the two variations automatically played while standing. */
export const RELAXED_STANCE_MOVES: readonly MoveId[] = ['idle', 'idle-shift', 'idle-scan'];
