import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { animateRotarySprinkler, createRotarySprinkler } from '../assets/rotarySprinkler.js';
import { collectSceneStats } from '../export/rotarySprinklerExportUtils.js';
const SOURCE_PATH = './source-glb/rotary-sprinkler.source.glb'; const STATIC_PATH = './optimized-glb/Rotary-Sprinkler.glb'; const MANIFEST_PATH = './manifests/rotary-sprinkler.export.json'; const RESULT_PATH = 'manifests/rotary-sprinkler.validation.json'; const GROUND_TOLERANCE = .006; const MAX_TRIANGLES = 2500; const DECODER_EXTENSIONS = new Set(['KHR_draco_mesh_compression', 'EXT_meshopt_compression']);
const assert = (condition, message) => { if (!condition) throw new Error(message); }; const check = (name, passed, details) => ({ name, passed, details }); const allPresent = (actual, expected) => expected.filter((name) => !new Set(actual).has(name)); const named = (root, predicate) => { const names = []; root.traverse((node) => { if (predicate(node)) names.push(node.name); }); return names.sort(); }; const finiteBounds = (bounds) => [...bounds.min, ...bounds.max, ...bounds.size, ...bounds.center].every(Number.isFinite);
async function readJson(path) { const response = await fetch(path, { cache: 'no-store' }); if (!response.ok) throw new Error(`Cannot read ${path}: ${response.status}`); return response.json(); } async function readExtensions(path) { const response = await fetch(path, { cache: 'no-store' }); if (!response.ok) throw new Error(`Cannot read ${path}: ${response.status}`); const data = await response.arrayBuffer(); const view = new DataView(data); assert(new TextDecoder().decode(new Uint8Array(data, 0, 4)) === 'glTF', `${path} is not a GLB.`); const jsonLength = view.getUint32(12, true); return JSON.parse(new TextDecoder().decode(new Uint8Array(data, 20, jsonLength)).trim()).extensionsUsed ?? []; } async function writeJson(path, value) { const response = await fetch(`/__artifact__?path=${encodeURIComponent(path)}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(value, null, 2) }); if (!response.ok) throw new Error(`Cannot write ${path}: ${await response.text()}`); }
export async function validateRotarySprinklerExports() { const [manifest, sourceGltf, optimizedGltf, sourceExtensions, optimizedExtensions] = await Promise.all([readJson(MANIFEST_PATH), new GLTFLoader().loadAsync(SOURCE_PATH), new GLTFLoader().loadAsync(STATIC_PATH), readExtensions(SOURCE_PATH), readExtensions(STATIC_PATH)]); const source = sourceGltf.scene; const optimized = optimizedGltf.scene; const sourceStats = collectSceneStats(source); const optimizedStats = collectSceneStats(optimized); const authored = createRotarySprinkler({ passId: 'interaction' }); const rotor = authored.userData.sculptRuntime.pivots['rotary-head']; const pedestal = authored.userData.sculptRuntime.pivots.riser; const restRotation = rotor.rotation.y; const fixedPedestalRotation = pedestal.rotation.y; animateRotarySprinkler(authored, 1.5, 1); const turnedRotation = rotor.rotation.y; const finalPedestalRotation = pedestal.rotation.y; authored.userData.sculptRuntime.dispose(); const sourceNodes = named(source, () => true); const sourceSockets = named(source, (node) => Boolean(node.userData.socket)); const sourceColliders = named(source, (node) => Boolean(node.userData.collider)); const sourceRoot = source.getObjectByName('rotary-sprinkler-root'); const semantics = sourceRoot?.userData?.sourceSemantics; const boundDelta = Math.max(...sourceStats.bounds.min.map((value, index) => Math.abs(value - optimizedStats.bounds.min[index])), ...sourceStats.bounds.max.map((value, index) => Math.abs(value - optimizedStats.bounds.max[index]))); const checks = [ check('source scene is nonempty', sourceStats.meshes > 0, { meshes: sourceStats.meshes }), check('optimized scene is nonempty', optimizedStats.meshes > 0, { meshes: optimizedStats.meshes }), check('source bounds are finite', finiteBounds(sourceStats.bounds), sourceStats.bounds), check('optimized bounds are finite', finiteBounds(optimizedStats.bounds), optimizedStats.bounds), check('source ground contact', Math.abs(sourceStats.bounds.min[1]) <= GROUND_TOLERANCE, { minY: sourceStats.bounds.min[1], tolerance: GROUND_TOLERANCE }), check('optimized ground contact', Math.abs(optimizedStats.bounds.min[1]) <= GROUND_TOLERANCE, { minY: optimizedStats.bounds.min[1], tolerance: GROUND_TOLERANCE }), check('source triangle budget', sourceStats.triangles <= MAX_TRIANGLES, { triangles: sourceStats.triangles, maxTriangles: MAX_TRIANGLES }), check('optimized triangle budget', optimizedStats.triangles <= MAX_TRIANGLES, { triangles: optimizedStats.triangles, maxTriangles: MAX_TRIANGLES }), check('source pivots survive GLTFLoader reload', allPresent(sourceNodes, manifest.source.requiredPivotNames).length === 0, { missing: allPresent(sourceNodes, manifest.source.requiredPivotNames) }), check('source sockets survive GLTFLoader reload', allPresent(sourceSockets, manifest.source.requiredSocketNames).length === 0, { missing: allPresent(sourceSockets, manifest.source.requiredSocketNames) }), check('source colliders survive GLTFLoader reload', allPresent(sourceColliders, manifest.source.requiredColliderNames).length === 0, { missing: allPresent(sourceColliders, manifest.source.requiredColliderNames) }), check('source action metadata survives GLTFLoader reload', Boolean(semantics) && semantics.animationChannels?.length === manifest.source.actionChannels.length && Object.keys(semantics.destructionGroups ?? {}).length === Object.keys(manifest.source.requiredDestructionGroups).length, { loadedAnimationChannels: semantics?.animationChannels?.length ?? 0, expectedAnimationChannels: manifest.source.actionChannels.length, loadedDestructionGroups: Object.keys(semantics?.destructionGroups ?? {}).length, expectedDestructionGroups: Object.keys(manifest.source.requiredDestructionGroups).length }), check('whole three-arm head rotates while pedestal stays fixed', Math.abs(turnedRotation - restRotation) > 2 && finalPedestalRotation === fixedPedestalRotation, { timeSeconds: 1.5, headRestRotation: restRotation, headRotation: turnedRotation, pedestalRestRotation: fixedPedestalRotation, pedestalRotation: finalPedestalRotation }), check('source has no decoder compression dependency', sourceExtensions.every((entry) => !DECODER_EXTENSIONS.has(entry)), { extensionsUsed: sourceExtensions }), check('optimized export has no decoder compression dependency', optimizedExtensions.every((entry) => !DECODER_EXTENSIONS.has(entry)), { extensionsUsed: optimizedExtensions }), check('optimized material groups survive reload', manifest.optimized.materials.length === 4 && allPresent(optimizedStats.materials, manifest.optimized.materials).length === 0, { expected: manifest.optimized.materials, loaded: optimizedStats.materials }), check('optimized bounds match source', boundDelta <= GROUND_TOLERANCE, { maxDelta: boundDelta, tolerance: GROUND_TOLERANCE }), check('optimized export reduces draw calls', optimizedStats.drawCalls < sourceStats.drawCalls, { source: sourceStats.drawCalls, optimized: optimizedStats.drawCalls }) ]; const result = { schemaVersion: 1, assetId: 'rotary-sprinkler', coordinateSystem: { up: 'Y', forward: '+Z', unit: 'metre', groundY: 0 }, result: checks.every((item) => item.passed) ? 'pass' : 'fail', checks, source: sourceStats, optimized: optimizedStats }; await writeJson(RESULT_PATH, result); return result; }
const output = document.querySelector('#output'); const write = (message) => { output.textContent += `${message}\n`; }; window.__ROTARY_SPRINKLER_VALIDATION_READY__ = false; try { write('Reloading Rotary Sprinkler GLBs with GLTFLoader…'); const result = await validateRotarySprinklerExports(); result.checks.forEach((item) => write(`${item.passed ? 'PASS' : 'FAIL'}: ${item.name}`)); write(`Validation result: ${result.result.toUpperCase()}`); window.__ROTARY_SPRINKLER_VALIDATION_RESULT__ = result; window.__ROTARY_SPRINKLER_VALIDATION_READY__ = true; } catch (error) { write(`Validation failed: ${error.stack || error.message}`); window.__ROTARY_SPRINKLER_VALIDATION_ERROR__ = String(error.stack || error.message); }
