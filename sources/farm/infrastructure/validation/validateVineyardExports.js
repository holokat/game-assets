import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js'; import { collectSceneStats } from '../export/handPumpExportUtils.js';
const SOURCE = 'source-glb/vineyard.source.glb', OPTIMIZED = 'optimized-glb/Vineyard.glb', EXPORT = 'manifests/vineyard.export.json', RESULT = 'manifests/vineyard.validation.json', TOLERANCE = 0.006, check = (name, passed, details = {}) => ({ name, passed, details }); const names = (root, predicate) => { const result = []; root.traverse((node) => { if (predicate(node)) result.push(node.name); }); return result.sort(); }, missing = (actual, expected) => expected.filter((value) => !new Set(actual).has(value)); async function read(path) { return (await fetch(path)).json(); } async function extensions(path) { const buffer = await (await fetch(path)).arrayBuffer(), view = new DataView(buffer), length = view.getUint32(12, true); return JSON.parse(new TextDecoder().decode(new Uint8Array(buffer, 20, length)).trim()).extensionsUsed ?? []; } async function save(value) { await fetch(`/__artifact__?path=${encodeURIComponent(RESULT)}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(value, null, 2) }); }
export async function validateVineyardExports() { const [manifest, sourceGltf, optimizedGltf, sourceExtensions, optimizedExtensions] = await Promise.all([read(EXPORT), new GLTFLoader().loadAsync(SOURCE), new GLTFLoader().loadAsync(OPTIMIZED), extensions(SOURCE), extensions(OPTIMIZED)]); const source = sourceGltf.scene, optimized = optimizedGltf.scene, sourceStats = collectSceneStats(source), optimizedStats = collectSceneStats(optimized), root = source.getObjectByName('vineyard-root'), semantics = root?.userData?.sourceSemantics, allNames = names(source, () => true), socketNames = names(source, (node) => Boolean(node.userData.socket)), colliderNames = names(source, (node) => Boolean(node.userData.collider)); const delta = Math.max(...sourceStats.bounds.min.map((value, index) => Math.abs(value - optimizedStats.bounds.min[index])), ...sourceStats.bounds.max.map((value, index) => Math.abs(value - optimizedStats.bounds.max[index]))), forbidden = new Set(['KHR_draco_mesh_compression', 'EXT_meshopt_compression']); const checks = [check('source scene is nonempty', sourceStats.meshes > 0, { meshes: sourceStats.meshes }), check('optimized scene is nonempty', optimizedStats.meshes > 0, { meshes: optimizedStats.meshes }), check('source ground contact', Math.abs(sourceStats.bounds.min[1]) <= TOLERANCE, { minY: sourceStats.bounds.min[1] }), check('optimized ground contact', Math.abs(optimizedStats.bounds.min[1]) <= TOLERANCE, { minY: optimizedStats.bounds.min[1] }), check('triangle budget', sourceStats.triangles < 5000 && optimizedStats.triangles < 5000, { source: sourceStats.triangles, optimized: optimizedStats.triangles }), check('optimized draw-call target', optimizedStats.drawCalls >= 3 && optimizedStats.drawCalls <= 6, { drawCalls: optimizedStats.drawCalls }), check('source pivots survive reload', missing(allNames, manifest.source.requiredPivotNames).length === 0, { missing: missing(allNames, manifest.source.requiredPivotNames) }), check('source sockets survive reload', missing(socketNames, manifest.source.requiredSocketNames).length === 0, { missing: missing(socketNames, manifest.source.requiredSocketNames) }), check('row colliders survive reload', missing(colliderNames, manifest.source.requiredColliderNames).length === 0, { missing: missing(colliderNames, manifest.source.requiredColliderNames) }), check('source static semantics survive reload', Boolean(semantics) && semantics.animationChannels?.length === 0, { channels: semantics?.animationChannels?.length ?? 0 }), check('no decoder dependency', sourceExtensions.every((value) => !forbidden.has(value)) && optimizedExtensions.every((value) => !forbidden.has(value)), { sourceExtensions, optimizedExtensions }), check('static bounds match source', delta <= TOLERANCE, { delta }), check('optimized export reduces draw calls', optimizedStats.drawCalls < sourceStats.drawCalls, { source: sourceStats.drawCalls, optimized: optimizedStats.drawCalls })]; const result = { schemaVersion: 1, assetId: 'vineyard', coordinateSystem: { up: 'Y', forward: '+Z', unit: 'metre', groundY: 0 }, result: checks.every((item) => item.passed) ? 'pass' : 'fail', checks, source: sourceStats, optimized: optimizedStats }; await save(result); return result; }
const output = document.querySelector('#output'); window.__VINEYARD_VALIDATION_READY__ = false; try { window.__VINEYARD_VALIDATION_RESULT__ = await validateVineyardExports(); output.textContent = JSON.stringify(window.__VINEYARD_VALIDATION_RESULT__, null, 2); window.__VINEYARD_VALIDATION_READY__ = true; } catch (error) { output.textContent = String(error.stack || error.message); window.__VINEYARD_VALIDATION_ERROR__ = String(error.stack || error.message); }
