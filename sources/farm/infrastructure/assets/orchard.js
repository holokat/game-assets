import * as THREE from 'three';import{addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';const m=(n,c)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:.8,flatShading:true,vertexColors:true});const q=(c,p,id,g,x,z)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),x),z);export function createOrchard(){const c=createAssetContext('orchard'),soil=m('orchard-soil-mat','#705236'),wood=m('orchard-trunk-mat','#76502c'),leaf=m('orchard-leaf-mat','#617946'),curb=m('orchard-curb-mat','#9a7650'),{model}=c;addSocket(c,model,'ground',[0,0,0]);const bed=addPivot(c,model,'orchard-bed',[0,.04,0]);q(c,bed,'soil-bed',new THREE.BoxGeometry(2.8,.08,1.9),soil,'soil');q(c,bed,'service-strip',new THREE.BoxGeometry(2.5,.025,.22),curb,'service');[[-1.2,-.5],[-.4,-.5],[.4,-.5],[1.2,-.5],[-.4,.5],[.4,.5]].forEach(([x,z],i)=>{const t=addPivot(c,bed,`tree-${i+1}`,[x,.15,z]);q(c,t,'tree-berm',new THREE.CylinderGeometry(.22,.25,.06,7),soil,'soil');q(c,t,'trunk',new THREE.CylinderGeometry(.04,.06,.55,6),wood,'trees').position.y=.3;const crown=q(c,t,'canopy',new THREE.ConeGeometry(.24,.43,6),leaf,'trees');crown.position.y=.72;});for(const[x,z,w,d]of[[0,-.97,2.9,.06],[0,.97,2.9,.06],[-1.43,0,.06,1.95],[1.43,0,.06,1.95]]){const e=q(c,bed,'curb',new THREE.BoxGeometry(w,.1,d),curb,'curb');e.position.set(x,.08,z);}for(const[x,id]of[[-1.5,'irrigation-in'],[1.5,'irrigation-out']]){const p=addPivot(c,bed,id,[x,.08,0]);q(c,p,id,new THREE.BoxGeometry(.12,.08,.2),curb,'irrigation');addSocket(c,p,id,[x,0,0]);}addSocket(c,bed,'terrain-anchor',[0,0,0]);addSocket(c,bed,'adjacency-north',[0,0,-1]);addCollider(c,bed,'orchard','box',[0,0,0],{width:2.9,height:1.1,depth:2,isTrigger:false});return finishAsset(c);}
