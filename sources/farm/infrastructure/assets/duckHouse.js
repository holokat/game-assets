import * as THREE from 'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';
const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true}),mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createDuckHouse(){const c=createAssetContext('duck-house'),{model}=c,stone=material('duck-house-stone-curb','#847c70'),wood=material('duck-house-waterproof-timber','#76583d'),roof=material('duck-house-galvanized-roof','#65757a',.42),iron=material('duck-house-dark-iron','#354044',.42),drain=material('duck-house-drainage-blue-gray','#6d9aa0',.5);addSocket(c,model,'ground',[0,0,0]);const house=addPivot(c,model,'low-duck-house-structure',[0,0,0]);const curb=mesh(c,house,'low-stone-curb',new THREE.BoxGeometry(2.65,.16,1.55),stone,'foundation');curb.position.y=.08;const body=mesh(c,house,'low-waterproof-shelter-body',new THREE.BoxGeometry(2.48,.62,1.42),wood,'structure');body.position.y=.47;for(const z of[-.27,.27]){const slope=mesh(c,house,'very-shallow-galvanized-roof',new THREE.BoxGeometry(2.78,.075,.82),roof,'roof');slope.position.set(0,.82,z);slope.rotation.x=z<0?.22:-.22;}const ridge=mesh(c,house,'roof-ridge',new THREE.BoxGeometry(2.73,.06,.08),iron,'roof');ridge.position.set(0,.9,0);const landing=mesh(c,house,'wide-landing-platform',new THREE.BoxGeometry(1.24,.12,.52),wood,'access');landing.position.set(0,.14,.95);const hatch=addPivot(c,house,'sealed-access-hatch',[0,.45,.745]);mesh(c,hatch,'small-waterproof-access-door',new THREE.BoxGeometry(.66,.42,.065),wood,'door');addChannel(c,hatch,'rotation','x',.55,.7,0);const ramp=mesh(c,house,'wide-slatted-access-ramp',new THREE.BoxGeometry(1.12,.08,.94),wood,'access');ramp.position.set(0,.27,1.54);ramp.rotation.x=-.36;for(let i=0;i<5;i++){const slat=mesh(c,house,'wide-ramp-slat',new THREE.BoxGeometry(1.2,.032,.06),stone,'access');slat.position.set(0,.16+i*.105,1.22+i*.17);slat.rotation.x=-.36;}for(const x of[-.72,.72]){const slot=mesh(c,house,'low-ventilation-slot',new THREE.BoxGeometry(.28,.12,.05),iron,'ventilation');slot.position.set(x,.58,.735);}const channel=mesh(c,house,'blue-gray-drainage-edge-cue',new THREE.BoxGeometry(2.55,.045,.28),drain,'drainage');channel.position.set(0,.023,-.94);const grate=mesh(c,house,'drainage-grate',new THREE.BoxGeometry(1.55,.025,.045),iron,'drainage');grate.position.set(0,.05,-.94);addSocket(c,house,'terrain-anchor',[0,0,0]);addSocket(c,house,'water',[0,.04,-1.1]);addSocket(c,house,'service',[1.32,.4,.3]);addSocket(c,house,'access',[0,.12,2.05]);addSocket(c,house,'attachment',[0,.9,0]);addSocket(c,house,'adjacency-east',[1.42,0,0]);addSocket(c,house,'drainage',[0,.04,-.94]);addCollider(c,house,'shelter','box',[0,.47,0],{width:2.48,height:.62,depth:1.42,isTrigger:false});addCollider(c,house,'platform','box',[0,.14,.95],{width:1.24,height:.12,depth:.52,isTrigger:false});addCollider(c,house,'ramp','box',[0,.27,1.54],{width:1.12,height:.16,depth:.94,isTrigger:false});return finishAsset(c);}export function animateDuckHouse(root,time){for(const ch of root?.userData?.sculptRuntime?.animationChannels||[])ch.node.rotation.x=ch.baseValue+Math.sin((Number.isFinite(time)?time:0)*ch.frequency+ch.phase)*ch.amplitude;return root;}
