import * as THREE from 'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';const mat=(n,c)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:.62,flatShading:true,vertexColors:true}),mesh=(c,p,id,g,m,gr)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),gr);export function createHandCart(){const c=createAssetContext('hand-cart'),{model}=c,wood=mat('hand-cart-deck-timber','#76583e'),iron=mat('hand-cart-dark-iron','#354145'),wheelY=.351890921741724;addSocket(c,model,'ground',[0,0,0]);const cart=addPivot(c,model,'lean-two-wheel-hand-cart',[0,0,0]),deck=mesh(c,cart,'empty-flat-wooden-deck',new THREE.BoxGeometry(1.6,.14,.9),wood,'deck');deck.position.y=.55;for(const x of[-.68,.68]){const rail=mesh(c,cart,'short-deck-rail',new THREE.BoxGeometry(.1,.35,.1),wood,'frame');rail.position.set(x,.78,.35)}const axle=mesh(c,cart,'two-wheel-axle',new THREE.BoxGeometry(.18,.1,1.25),iron,'wheel');axle.position.set(0,wheelY,0);const wp=addPivot(c,cart,'visible-cart-wheels-pivot',[0,wheelY,0]);for(const z of[-.58,.58]){const wheel=mesh(c,wp,'dark-iron-cart-wheel',new THREE.TorusGeometry(.3,.07,5,10),iron,'wheel');wheel.position.z=z;wheel.rotation.y=Math.PI/2}addChannel(c,wp,'rotation','x',Math.PI,.7,0);const handle=mesh(c,cart,'slim-timber-pull-handle',new THREE.BoxGeometry(1.25,.08,.12),wood,'frame');handle.position.set(1.3,.35,0);handle.rotation.z=-.24;const leg=mesh(c,cart,'pull-handle-support-leg',new THREE.BoxGeometry(.1,.45,.1),wood,'frame');leg.position.set(.85,.23,0);addSocket(c,cart,'terrain-anchor',[0,0,0]);addSocket(c,cart,'load',[0,.7,0]);addSocket(c,cart,'pull-handle',[1.9,.35,0]);addSocket(c,cart,'attachment',[0,wheelY,0]);addSocket(c,cart,'ground',[0,0,0]);addCollider(c,cart,'cart-deck','box',[0,.55,0],{width:1.6,height:.14,depth:.9,isTrigger:false});addCollider(c,cart,'wheels','box',[0,wheelY,0],{width:.2,height:.62,depth:1.3,isTrigger:false});return finishAsset(c)}export function animateHandCart(r,t){for(const c of r.userData.sculptRuntime.animationChannels)c.node.rotation.x=c.baseValue+Math.sin(t*c.frequency)*c.amplitude;return r}
