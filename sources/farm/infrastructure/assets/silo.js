import * as THREE from 'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';
const material=(name,color,roughness=.6)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true});const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createSilo(){const c=createAssetContext('silo'),{model}=c,galv=material('silo-galvanized','#829094',.42),roof=material('silo-roof','#606c70',.45),iron=material('silo-dark-steel','#354044',.42),stone=material('silo-stone-feet','#8a8374');addSocket(c,model,'ground',[0,0,0]);const b=addPivot(c,model,'silo-body',[0,0,0]);const hopper=mesh(c,b,'hopper-cone',new THREE.ConeGeometry(.64,.58,10,1,true),galv,'body');hopper.position.y=1.23;hopper.rotation.x=Math.PI;const body=mesh(c,b,'faceted-cylinder-body',new THREE.CylinderGeometry(.64,.64,1.85,10,1,false),galv,'body');body.position.y=2.45;const cap=mesh(c,b,'conical-roof',new THREE.ConeGeometry(.72,.54,10),roof,'roof');cap.position.y=3.64;for(const y of[1.84,2.95]){const band=mesh(c,b,'reinforcing-band',new THREE.TorusGeometry(.665,.027,4,10),iron,'body');band.position.y=y;band.rotation.x=Math.PI/2;}for(const[x,z]of[[-.46,-.34],[.46,-.34],[-.46,.34],[.46,.34]]){const foot=mesh(c,b,'stone-foot',new THREE.BoxGeometry(.23,.12,.23),stone,'legs');foot.position.set(x,.06,z);const leg=mesh(c,b,'slender-steel-leg',new THREE.CylinderGeometry(.045,.055,1.02,6),iron,'legs');leg.position.set(x,.57,z);leg.rotation.z=x<0?-.12:.12;}const chute=mesh(c,b,'grain-chute',new THREE.CylinderGeometry(.13,.1,.42,8),galv,'chute');chute.position.set(0,.72,.1);const gate=addPivot(c,b,'grain-gate-handle',[.16,.67,.12]);mesh(c,gate,'gate-handle',new THREE.BoxGeometry(.28,.055,.055),iron,'chute');addChannel(c,gate,'rotation','y',.75,.8,0);const pipe=mesh(c,b,'fill-pipe',new THREE.CylinderGeometry(.075,.075,1.35,7),galv,'fill');pipe.position.set(.78,3.0,-.16);pipe.rotation.z=-.24;const pipeCap=mesh(c,b,'fill-cap',new THREE.CylinderGeometry(.1,.1,.06,7),roof,'fill');pipeCap.position.set(.94,3.65,-.16);const ladder=addPivot(c,b,'external-ladder',[.66,2.25,.33]);for(const x of[-.12,.12]){const rail=mesh(c,ladder,'ladder-rail',new THREE.CylinderGeometry(.027,.027,1.65,6),iron,'ladder');rail.position.x=x;}for(let i=0;i<6;i++){const rung=mesh(c,ladder,'ladder-rung',new THREE.CylinderGeometry(.02,.02,.28,5),iron,'ladder');rung.position.y=-.7+i*.27;rung.rotation.z=Math.PI/2;}for(const y of[.35,.75]){const hoop=mesh(c,ladder,'ladder-safety-hoop',new THREE.TorusGeometry(.2,.02,4,8,Math.PI),iron,'ladder');hoop.position.y=y;hoop.rotation.y=Math.PI/2;}addSocket(c,b,'terrain-anchor',[0,0,0]);addSocket(c,b,'fill',[.93,3.65,-.16]);addSocket(c,b,'grain-output',[0,.48,.1]);addSocket(c,b,'service',[.72,2.3,.35]);addSocket(c,b,'attachment',[0,3.9,0]);addSocket(c,b,'adjacency-east',[.8,0,0]);addCollider(c,b,'body','cylinder',[0,2.45,0],{radius:.64,height:1.85,isTrigger:false});addCollider(c,b,'legs','box',[0,.6,0],{width:1.1,height:1.2,depth:.85,isTrigger:false});addCollider(c,b,'chute','capsule',[0,.75,.1],{radius:.14,height:.45,isTrigger:false});return finishAsset(c);}export function animateSilo(root,time){const ch=root?.userData?.sculptRuntime?.animationChannels?.[0];if(ch)ch.node.rotation.y=Math.sin((Number.isFinite(time)?time:0)*ch.frequency)*ch.amplitude;return root;}
