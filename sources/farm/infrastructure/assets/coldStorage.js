import * as THREE from 'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true}),mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createColdStorage(){const c=createAssetContext('cold-storage'),{model}=c,panel=material('cold-storage-pale-panel','#b7c4c4'),stone=material('cold-storage-stone-curb','#888174'),roof=material('cold-storage-galvanized-roof','#657277',.42),iron=material('cold-storage-dark-frame','#354044',.42),cool=material('cold-storage-refrigeration-box','#6f9aa1',.48);addSocket(c,model,'ground',[0,0,0]);const b=addPivot(c,model,'cold-storage-building',[0,0,0]);const curb=mesh(c,b,'stone-curb',new THREE.BoxGeometry(2.55,.18,1.45),stone,'building');curb.position.y=.09;const shell=mesh(c,b,'insulated-panel-body',new THREE.BoxGeometry(2.42,1.2,1.32),panel,'building');shell.position.y=.78;const roofPanel=mesh(c,b,'shallow-galvanized-roof',new THREE.BoxGeometry(2.68,.1,1.57),roof,'roof');roofPanel.position.y=1.43;for(const x of[-1.16,1.16])for(const z of[-.6,.6]){const trim=mesh(c,b,'dark-frame-trim',new THREE.BoxGeometry(.055,1.3,.055),iron,'frame');trim.position.set(x,.77,z);}const platform=mesh(c,b,'covered-loading-platform',new THREE.BoxGeometry(1.45,.18,.52),stone,'loading');platform.position.set(-.35,.09,.92);const door=addPivot(c,b,'sliding-loading-door',[0,.82,.69]);mesh(c,door,'wide-insulated-loading-door',new THREE.BoxGeometry(1.35,.92,.07),panel,'door');addChannel(c,door,'position','x',.7,.8,0);const rail=mesh(c,b,'door-rail',new THREE.BoxGeometry(1.7,.05,.07),iron,'door');rail.position.set(0,1.3,.72);const service=mesh(c,b,'small-service-door',new THREE.BoxGeometry(.45,.82,.07),panel,'door');service.position.set(.9,.62,.69);for(const x of[-.9,.88]){const box=mesh(c,b,'external-refrigeration-box',new THREE.BoxGeometry(.42,.52,.3),cool,'cooling');box.position.set(x,.82,-.82);const grille=mesh(c,b,'fan-grille',new THREE.TorusGeometry(.13,.018,4,8),iron,'cooling');grille.position.set(x,.82,-.99);grille.rotation.x=Math.PI/2;const fan=mesh(c,b,'fan-rotor',new THREE.BoxGeometry(.22,.018,.035),iron,'cooling');fan.position.set(x,.82,-1.01);}const control=mesh(c,b,'temperature-control-box',new THREE.BoxGeometry(.22,.32,.12),iron,'service');control.position.set(1.28,.85,.2);addSocket(c,b,'terrain-anchor',[0,0,0]);addSocket(c,b,'loading',[-.35,.2,1.2]);addSocket(c,b,'storage',[0,.75,0]);addSocket(c,b,'power',[1.34,.85,.2]);addSocket(c,b,'cooling',[-.9,.82,-1]);addSocket(c,b,'service',[1.3,.32,.35]);addSocket(c,b,'attachment',[0,1.48,0]);addSocket(c,b,'adjacency-east',[1.35,0,0]);addCollider(c,b,'building','box',[0,.78,0],{width:2.42,height:1.2,depth:1.32,isTrigger:false});addCollider(c,b,'platform','box',[-.35,.09,.92],{width:1.45,height:.18,depth:.52,isTrigger:false});addCollider(c,b,'door','box',[0,.82,.69],{width:1.35,height:.92,depth:.08,isTrigger:false});return finishAsset(c);}export function animateColdStorage(root,time){const ch=root?.userData?.sculptRuntime?.animationChannels?.[0];if(ch)ch.node.position.x=Math.sin((Number.isFinite(time)?time:0)*ch.frequency)*ch.amplitude;return root;}
