import * as THREE from 'three';import {addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';import {faceted} from '../core/geometryLibrary.js';const mat=(n,c,r=.7,m=0)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:r,metalness:m,flatShading:true,vertexColors:true});const M=(c,p,i,g,x,d)=>registerMesh(c,p,i,new THREE.Mesh(faceted(g,`${c.id}:${i}`),x),d,'optimization-pass');const B=(c,p,i,s,o,x,d)=>{const q=M(c,p,i,new THREE.BoxGeometry(...s),x,d);q.position.set(...o);return q};
export function createWaterWheel(){const c=createAssetContext('water-wheel',{label:'Water Wheel',targetHeightMetres:3.1,passId:'optimization-pass'}),{model}=c,x={teal:mat('wheel-muted-teal','#4e7674',.62,.1),iron:mat('wheel-dark-iron','#30393a',.5,.3),timber:mat('wheel-timber','#8b6f51',.84),cream:mat('wheel-cream','#d0c2aa',.78),galv:mat('wheel-galvanized','#8c9995',.46,.28)};const frame=addPivot(c,model,'fixed-frame'),wheel=addPivot(c,frame,'water-wheel-rotor',[0,1.55,0]);B(c,frame,'stone-channel-base',[3.1,.3,1.7],[0,.15,0],x.cream,'channel');for(const dx of [-1.1,1.1])B(c,frame,`support-frame-${dx}`,[.18,2.7,.35],[dx,1.5,0],x.iron,'frame');const ring=M(c,wheel,'large-timber-wheel',new THREE.TorusGeometry(.95,.13,8,16),x.timber,'wheel');ring.rotation.y=Math.PI/2;for(let n=0;n<8;n++){const a=n*Math.PI/4,blade=B(c,wheel,`water-blade-${n}`,[.14,.38,.42],[0,0,0],x.teal,'wheel');blade.position.set(Math.cos(a)*.82,Math.sin(a)*.82,0);blade.rotation.z=-a;}const axle=M(c,wheel,'wheel-axle',new THREE.CylinderGeometry(.13,.13,2.55,8),x.galv,'wheel');axle.rotation.z=Math.PI/2;addChannel(c,wheel,'rotation','z',Math.PI*2,.22,0);B(c,frame,'water-inlet-lip',[1.2,.18,.5],[0,1.85,.6],x.galv,'channel');for(const [i,o] of [['ground',[0,0,0]],['water-in',[0,1.75,1.1]],['water-out',[0,.35,-1.1]],['power-shaft',[1.4,1.55,0]],['service',[-1.4,.5,0]]])addSocket(c,model,i,o);addCollider(c,frame,'wheel-frame','box',[0,1.45,0],{width:2.6,height:2.8,depth:1.7,isTrigger:false});addCollider(c,wheel,'wheel','cylinder',[0,0,0],{radius:1.1,height:.32,axis:'z',isTrigger:false});const r=finishAsset(c);r.userData.waterWheelRig={wheel,frame};r.userData.artDirection={concept:'references/concepts/water-wheel.png',identity:['large timber water wheel','eight visible paddles','stone channel','iron support frame','power axle'],motion:'Only water wheel rotor turns.'};r.userData.qualityContract={triangleBudget:5000,optimizedDrawRange:[4,6],criticalFeatures:['large wheel','paddles','stone channel','support frame']};return r;}
export function animateWaterWheel(r,t=0){if(r?.userData?.waterWheelRig)r.userData.waterWheelRig.wheel.rotation.z=t*Math.PI*2*.22;return r;}
