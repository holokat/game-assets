import * as THREE from 'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';
const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true}),mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createHayloft(){const c=createAssetContext('hayloft'),{model}=c,wood=material('hayloft-timber','#644d35'),roof=material('hayloft-galvanized-roof','#637175',.42),iron=material('hayloft-hoist-iron','#354044',.42),stone=material('hayloft-stone-feet','#898173'),hay=material('hayloft-hay-bales','#b68d43');addSocket(c,model,'ground',[0,0,0]);const b=addPivot(c,model,'hayloft-frame',[0,0,0]);for(const x of[-.86,.86])for(const z of[-.48,0,.48]){const foot=mesh(c,b,'stone-foot',new THREE.BoxGeometry(.22,.12,.22),stone,'posts');foot.position.set(x,.06,z);const post=mesh(c,b,'six-timber-post',new THREE.BoxGeometry(.11,1.12,.11),wood,'posts');post.position.set(x,.62,z);}const floor=mesh(c,b,'raised-plank-floor',new THREE.BoxGeometry(1.92,.12,1.35),wood,'platform');floor.position.y=1.17;for(const z of[-.59,.59])for(let y=0;y<3;y++){const slat=mesh(c,b,'half-height-wall-slat',new THREE.BoxGeometry(1.82,.11,.06),wood,'walls');slat.position.set(0,1.35+y*.18,z);}for(const z of[-.34,.34]){const p=mesh(c,b,'pitched-roof-panel',new THREE.BoxGeometry(2.18,.07,.8),roof,'roof');p.position.set(0,2.1,z);p.rotation.z=z<0?-.36:.36;}for(const x of[-.78,.78]){const rail=mesh(c,b,'ladder-rail',new THREE.BoxGeometry(.06,1.12,.06),wood,'ladder');rail.position.set(x, .57,.78);rail.rotation.x=.28;}for(let i=0;i<5;i++){const rung=mesh(c,b,'ladder-rung',new THREE.BoxGeometry(1.5,.05,.06),wood,'ladder');rung.position.set(0,.22+i*.2,.78);}const beam=mesh(c,b,'projecting-hoist-beam',new THREE.BoxGeometry(.12,.12,1.0),wood,'hoist');beam.position.set(0,2.05,.88);const pulley=mesh(c,b,'hoist-pulley',new THREE.TorusGeometry(.14,.032,4,8),iron,'hoist');pulley.position.set(0,1.86,1.22);pulley.rotation.x=Math.PI/2;const winch=addPivot(c,b,'hand-winch-control',[.98,1.55,.35]);mesh(c,winch,'hand-winch-wheel',new THREE.TorusGeometry(.15,.035,4,8),iron,'hoist');winch.rotation.y=Math.PI/2;addChannel(c,winch,'rotation','y',1.15,.8,0);for(const[x,z,y]of[[-.45,-.25,1.39],[0,-.25,1.39],[.45,-.25,1.39],[-.45,.23,1.39],[0,.23,1.39],[.45,.23,1.39]]){const bale=mesh(c,b,'angular-bound-hay-bale',new THREE.BoxGeometry(.38,.24,.33),hay,'bales');bale.position.set(x,y,z);const bind=mesh(c,b,'bale-binding',new THREE.BoxGeometry(.035,.26,.35),iron,'bales');bind.position.set(x,y,z);}addSocket(c,b,'terrain-anchor',[0,0,0]);addSocket(c,b,'loading',[0,1.22,1.05]);addSocket(c,b,'storage',[0,1.43,0]);addSocket(c,b,'hoist',[0,1.86,1.22]);addSocket(c,b,'attachment',[0,2.22,0]);addSocket(c,b,'adjacency-east',[1.05,0,0]);addCollider(c,b,'posts','box',[0,.62,0],{width:1.82,height:1.24,depth:1.24,isTrigger:false});addCollider(c,b,'platform','box',[0,1.17,0],{width:1.92,height:.12,depth:1.35,isTrigger:false});addCollider(c,b,'bales','box',[0,1.42,0],{width:1.4,height:.35,depth:.8,isTrigger:false});return finishAsset(c);}export function animateHayloft(root,time){const ch=root?.userData?.sculptRuntime?.animationChannels?.[0];if(ch)ch.node.rotation.y=Math.sin((Number.isFinite(time)?time:0)*ch.frequency)*ch.amplitude;return root;}
