import * as THREE from 'three';
import {addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';
import {faceted} from '../core/geometryLibrary.js';
const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true});
const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createPress(){
 const c=createAssetContext('press'),{model}=c,stone=material('press-drainage-base','#847b6d'),timber=material('press-weathered-timber','#76583e'),iron=material('press-dark-iron-frame','#354145',.42),pan=material('press-collection-pan','#9aa2a0',.42),canopy=material('press-weather-canopy','#68767a',.42);
 addSocket(c,model,'ground',[0,0,0]);const press=addPivot(c,model,'generic-fruit-press-station',[0,0,0]);const base=mesh(c,press,'drainage-service-base',new THREE.BoxGeometry(2.25,.16,1.7),stone,'foundation');base.position.y=.08;
 for(const x of[-.72,.72]){const post=mesh(c,press,'upright-timber-press-frame',new THREE.BoxGeometry(.12,1.52,.12),timber,'frame');post.position.set(x,.86,0);const top=mesh(c,press,'press-frame-crossbeam',new THREE.BoxGeometry(1.62,.13,.14),timber,'frame');top.position.set(0,1.52,0);}
 const basket=mesh(c,press,'shallow-empty-fruit-input-basket-cue',new THREE.CylinderGeometry(.43,.43,.44,8),timber,'input');basket.position.set(0,.68,0);const panMesh=mesh(c,press,'shallow-collection-pan',new THREE.CylinderGeometry(.58,.58,.1,12),pan,'collection');panMesh.position.set(0,.39,0);const center=addPivot(c,press,'visible-press-screw-ram',[0,1.28,0]);const screw=mesh(c,center,'central-press-screw',new THREE.CylinderGeometry(.09,.09,.72,8),iron,'press');screw.position.y=-.3;const plate=mesh(c,center,'press-plate',new THREE.CylinderGeometry(.34,.34,.08,8),iron,'press');plate.position.y=-.65;const wheel=mesh(c,center,'press-handwheel',new THREE.TorusGeometry(.22,.04,5,8),iron,'press');wheel.rotation.x=Math.PI/2;addChannel(c,center,'position','y',.16,.65,0);
 const spout=mesh(c,press,'short-juice-output-spout',new THREE.CylinderGeometry(.07,.07,.62,6),iron,'output');spout.position.set(.58,.33,.18);spout.rotation.z=Math.PI/2;const waste=mesh(c,press,'small-waste-output',new THREE.BoxGeometry(.2,.18,.32),timber,'output');waste.position.set(-.7,.3,-.42);const drain=mesh(c,press,'base-drain-cue',new THREE.BoxGeometry(1.05,.03,.13),iron,'drainage');drain.position.set(0,.18,.58);const canopyMesh=mesh(c,press,'slim-weather-canopy',new THREE.BoxGeometry(1.95,.07,1.45),canopy,'canopy');canopyMesh.position.set(0,1.74,0);canopyMesh.rotation.x=.08;
 addSocket(c,press,'terrain-anchor',[0,0,0]);addSocket(c,press,'fruit-input',[0,.9,0]);addSocket(c,press,'press-center',[0,.7,0]);addSocket(c,press,'juice-output',[1.0,.33,.18]);addSocket(c,press,'waste-output',[-.85,.3,-.42]);addSocket(c,press,'drain',[0,.18,.7]);addSocket(c,press,'service',[.72,.7,-.5]);addSocket(c,press,'attachment',[0,1.75,0]);addSocket(c,press,'adjacency-east',[1.25,0,0]);addSocket(c,press,'ground',[0,0,0]);
 addCollider(c,press,'press-frame','box',[0,.86,0],{width:1.62,height:1.55,depth:.7,isTrigger:false});addCollider(c,press,'fruit-basket','box',[0,.68,0],{width:.86,height:.44,depth:.86,isTrigger:false});addCollider(c,press,'collection-pan','box',[0,.39,0],{width:1.16,height:.12,depth:1.16,isTrigger:false});addCollider(c,press,'service-base','box',[0,.08,0],{width:2.25,height:.16,depth:1.7,isTrigger:false});return finishAsset(c);
}
export function animatePress(root,time){for(const ch of root?.userData?.sculptRuntime?.animationChannels||[])ch.node.position.y=ch.baseValue+Math.sin((Number.isFinite(time)?time:0)*ch.frequency+ch.phase)*ch.amplitude;return root;}
