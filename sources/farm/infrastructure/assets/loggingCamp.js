import * as THREE from 'three';
import {addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';
import {faceted} from '../core/geometryLibrary.js';
const mat=(name,color,roughness=.72,metalness=0)=>new THREE.MeshStandardMaterial({name,color,roughness,metalness,flatShading:true,vertexColors:true});
const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group,'optimization-pass');
const box=(c,p,id,s,pos,m,group)=>{const q=mesh(c,p,id,new THREE.BoxGeometry(...s),m,group);q.position.set(...pos);return q};
const post=(c,p,id,r,h,pos,m,group)=>{const q=mesh(c,p,id,new THREE.CylinderGeometry(r,r,h,8),m,group);q.position.set(...pos);return q};
export function createLoggingCamp(){const c=createAssetContext('logging-camp',{label:'Logging Camp',passId:'optimization-pass'}),{model}=c,m={earth:mat('logging-earth','#a2794d',.94),timber:mat('logging-timber','#70573d',.84),roof:mat('logging-teal-roof','#356d70',.58,.18),metal:mat('logging-metal','#303b3b',.42,.55),cut:mat('logging-cut-end','#b6814b',.9)},fixed=addPivot(c,model,'fixed-camp');box(c,fixed,'dirt-pad',[6.4,.08,4.8],[0,.04,0],m.earth,'ground');box(c,fixed,'sorting-deck',[4.6,.24,2.7],[-.55,.44,.15],m.timber,'deck');for(const x of[-2.6,1.5])for(const z of[-1,1.25])post(c,fixed,`shelter-post-${x}-${z}`,.12,2.35,[x,1.62,z],m.timber,'shelter');box(c,fixed,'roof-main',[4.95,.18,2.9],[-.55,2.83,.15],m.roof,'shelter');for(const x of[-2.5,-.55,1.4]){const b=box(c,fixed,`roof-rib-${x}`,[.08,.12,3.05],[x,2.94,.15],m.metal,'shelter');}for(const z of[-.6,.55])box(c,fixed,`deck-rail-${z}`,[4.35,.12,.1],[-.55,1.2,z],m.metal,'deck');for(const x of[-2.75,-2.1])for(const z of[-1.5,-.8]){post(c,fixed,`rack-post-${x}-${z}`,.1,.8,[x,.45,z],m.timber,'rack');}for(let i=0;i<5;i++){const log=post(c,fixed,`stored-log-${i}`,.16,1.15,[-2.42,.34+(i%2)*.3,-1.15+(i%3)*.24],m.cut,'logs');log.rotation.z=Math.PI/2;}
box(c,fixed,'locker-body',[1.05,1.6,1.1],[2.18,.84,.75],m.timber,'locker');box(c,fixed,'locker-roof',[1.22,.16,1.26],[2.18,1.7,.75],m.roof,'locker');box(c,fixed,'locker-door',[.06,1.25,.7],[1.63,.8,.75],m.metal,'locker');for(let i=0;i<3;i++)box(c,fixed,`stair-${i}`,[1.05,.12,.36],[.05,.15+i*.14,-1.48+i*.26],m.timber,'deck');for(const[id,pos]of[['ground',[0,0,0]],['road',[0,.08,-2.55]],['service',[3.1,.08,.6]],['logs',[-2.45,.1,-1.3]],['deck',[-.55,.56,.15]]])addSocket(c,model,id,pos);addCollider(c,fixed,'camp','box',[0,.45,0],{width:6.4,height:.9,depth:4.8,isTrigger:false});return finishAsset(c);}
