import * as THREE from 'three';
import {addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';
import {faceted} from '../core/geometryLibrary.js';
const m=(n,c,r=.75,x=0)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:r,metalness:x,flatShading:true,vertexColors:true});
const q=(c,p,id,g,a,d)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),a),d,'optimization-pass');
const b=(c,p,id,s,v,a,d)=>{const o=q(c,p,id,new THREE.BoxGeometry(...s),a,d);o.position.set(...v);return o};const cy=(c,p,id,r,h,v,a,d)=>{const o=q(c,p,id,new THREE.CylinderGeometry(r,r,h,8),a,d);o.position.set(...v);return o};
export function createWoodPile(){const c=createAssetContext('wood-pile',{label:'Wood Pile',passId:'optimization-pass'}),{model}=c,a={earth:m('woodpile-earth','#a2794d',.94),timber:m('woodpile-timber','#6d563c',.84),roof:m('woodpile-teal-roof','#356d70',.58,.18),metal:m('woodpile-metal','#303b3b',.42,.55),cut:m('woodpile-cut','#bc874b',.9)},fixed=addPivot(c,model,'fixed-pile');b(c,fixed,'dirt-pad',[4.7,.08,2.5],[0,.04,0],a.earth,'ground');for(const x of[-1.9,1.9])for(const z of[-.8,.8])cy(c,fixed,`post-${x}-${z}`,.11,2.15,[x,1.1,z],a.timber,'rack');b(c,fixed,'roof-main',[4.45,.16,2.1],[0,2.22,0],a.roof,'rack');for(const x of[-1.8,-.9,0,.9,1.8])b(c,fixed,`roof-rib-${x}`,[.07,.11,2.18],[x,2.33,0],a.metal,'rack');for(const y of[.34,.67,1.0,1.33])for(let i=0;i<7;i++){const log=cy(c,fixed,`split-log-${y}-${i}`,.14,.54,[-1.5+i*.5,y,(i%2)*.25-.18],a.cut,'logs');log.rotation.z=Math.PI/2;}for(const z of[-.88,.88])b(c,fixed,`lower-rail-${z}`,[4,.1,.1],[0,.25,z],a.timber,'rack');for(const[id,pos]of[['ground',[0,0,0]],['front',[0,.08,-1.35]],['service',[2.3,.08,0]],['stock',[0,.4,0]]])addSocket(c,model,id,pos);addCollider(c,fixed,'pile','box',[0,1,0],{width:4.4,height:2,depth:2.2,isTrigger:false});return finishAsset(c);}
