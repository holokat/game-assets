import * as THREE from 'three';
import {addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';
import {faceted} from '../core/geometryLibrary.js';
const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true});
const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createManurePit(){
 const c=createAssetContext('manure-pit'),{model}=c,concrete=material('manure-pit-reinforced-concrete','#87847b'),inside=material('manure-pit-dark-contained-interior','#3b3933',.6),iron=material('manure-pit-dark-guard-rail','#354145',.42),cover=material('manure-pit-inlet-cover','#6c746f',.42),service=material('manure-pit-service-box','#917d60');
 addSocket(c,model,'ground',[0,0,0]);const pit=addPivot(c,model,'compact-recessed-manure-pit',[0,0,0]);const slab=mesh(c,pit,'reinforced-concrete-pit-slab',new THREE.BoxGeometry(2.9,.16,2.35),concrete,'foundation');slab.position.y=.08;const interior=mesh(c,pit,'dark-contained-pit-interior-cue',new THREE.BoxGeometry(2.15,.05,1.62),inside,'interior');interior.position.y=.18;
 for(const[x,z,sx,sz]of[[0,-.97,2.9,.32],[0,.97,2.9,.32],[-1.29,0,.32,2.35],[1.29,0,.32,2.35]]){const curb=mesh(c,pit,'low-reinforced-safety-curb',new THREE.BoxGeometry(sx,.32,sz),concrete,'curb');curb.position.set(x,.16,z);}for(const x of[-.95,.95])for(const z of[-.68,.68]){const post=mesh(c,pit,'compact-safety-guard-post',new THREE.BoxGeometry(.07,.58,.07),iron,'safety');post.position.set(x,.48,z);}const rail=mesh(c,pit,'front-safety-guard-rail',new THREE.BoxGeometry(1.95,.07,.07),iron,'safety');rail.position.set(0,.69,.68);
 const chute=mesh(c,pit,'covered-manure-inlet-chute',new THREE.BoxGeometry(.65,.38,.78),cover,'inlet');chute.position.set(-1.6,.38,-.35);chute.rotation.x=.25;const collar=mesh(c,pit,'inlet-chute-collar',new THREE.BoxGeometry(.28,.25,.28),iron,'inlet');collar.position.set(-1.25,.35,-.35);
 const platform=mesh(c,pit,'small-service-pump-platform',new THREE.BoxGeometry(.62,.15,.58),service,'service');platform.position.set(1.62,.25,-.36);const box=mesh(c,pit,'service-pump-valve-box',new THREE.BoxGeometry(.34,.42,.3),service,'service');box.position.set(1.62,.53,-.36);const valve=addPivot(c,pit,'service-isolation-valve-lever',[1.62,.78,-.36]);const handle=mesh(c,valve,'visible-isolation-valve-handle',new THREE.BoxGeometry(.36,.055,.07),iron,'pump');handle.position.x=.18;addChannel(c,valve,'rotation','y',.42,.75,0);const removal=mesh(c,pit,'removal-drain-access-pipe',new THREE.CylinderGeometry(.1,.1,.68,6),iron,'drain');removal.position.set(1.58,.28,.72);removal.rotation.z=Math.PI/2;
 addSocket(c,pit,'terrain-anchor',[0,0,0]);addSocket(c,pit,'inlet',[-1.95,.38,-.35]);addSocket(c,pit,'removal',[1.98,.28,.72]);addSocket(c,pit,'drain',[0,.18,-1.2]);addSocket(c,pit,'pump',[1.62,.53,-.36]);addSocket(c,pit,'service',[1.62,.53,-.36]);addSocket(c,pit,'attachment',[0,.45,0]);addSocket(c,pit,'adjacency-east',[1.6,0,0]);addSocket(c,pit,'ground',[0,0,0]);
 addCollider(c,pit,'collection-pit','box',[0,.18,0],{width:2.2,height:.22,depth:1.65,isTrigger:true});addCollider(c,pit,'concrete-curb','box',[0,.16,0],{width:2.9,height:.32,depth:2.35,isTrigger:false});addCollider(c,pit,'inlet-chute','box',[-1.6,.38,-.35],{width:.65,height:.38,depth:.78,isTrigger:false});addCollider(c,pit,'pump-service','box',[1.62,.45,-.36],{width:.62,height:.55,depth:.58,isTrigger:false});return finishAsset(c);
}
export function animateManurePit(root,time){for(const ch of root?.userData?.sculptRuntime?.animationChannels||[])ch.node.rotation.y=ch.baseValue+Math.sin((Number.isFinite(time)?time:0)*ch.frequency+ch.phase)*ch.amplitude;return root;}
