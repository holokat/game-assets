import*as T from'three';import{addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';const M=(n,c)=>new T.MeshStandardMaterial({name:n,color:c,roughness:.85,flatShading:true}),q=(c,p,id,s,x,g)=>registerMesh(c,p,id,new T.Mesh(new T.BoxGeometry(...s),x),g,'structural-pass');export function createStoneWall(){const c=createAssetContext('stone-wall',{label:'Stone Wall'}),s=M('wall-stone','#716b62'),a=addPivot(c,c.model,'segment');for(let row=0;row<3;row++)for(let x=-1.6;x<=1.6;x+=.8){let n=q(c,a,`stone-${row}-${x}`,[.72,.38,.45],s,'masonry');n.position.set(x+(row%2?.18:0),.19+row*.38,0)}addSocket(c,c.model,'ground',[0,0,0]);addSocket(c,c.model,'left-end-socket',[-2,0,0]);addSocket(c,c.model,'right-end-socket',[2,0,0]);addCollider(c,a,'wall','box',[0,.57,0],{width:4,height:1.2,depth:.5,isTrigger:false});return finishAsset(c)}
