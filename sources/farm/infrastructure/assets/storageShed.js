import * as THREE from 'three';
import { addChannel, addCollider, addPivot, addSocket, createAssetContext, finishAsset, registerMesh } from '../core/assetContext.js';
import { faceted } from '../core/geometryLibrary.js';
const material=(name,color,roughness=.65)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true});
const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createStorageShed(){const c=createAssetContext('storage-shed'),{model}=c,stone=material('storage-shed-stone-curb','#8b8272'),timber=material('storage-shed-timber','#66503a'),roof=material('storage-shed-galvanized-roof','#657276',.42),iron=material('storage-shed-iron','#30393b',.4),shelf=material('storage-shed-interior-shelf','#896947');addSocket(c,model,'ground',[0,0,0]);const b=addPivot(c,model,'shed-body',[0,0,0]);mesh(c,b,'stone-curb',new THREE.BoxGeometry(2.25,.18,1.35),stone,'building').position.y=.09;const back=mesh(c,b,'rear-wall',new THREE.BoxGeometry(2.05,1.16,.08),timber,'building');back.position.set(0,.74,-.59);for(const x of[-1.0,1.0]){const side=mesh(c,b,'side-wall',new THREE.BoxGeometry(.08,1.16,1.2),timber,'building');side.position.set(x,.74,0);}for(const z of[-.36,.36]){const roofPanel=mesh(c,b,'pitched-roof',new THREE.BoxGeometry(2.35,.08,.76),roof,'roof');roofPanel.position.set(0,1.37,z);roofPanel.rotation.z=z<0?-.37:.37;}for(const x of[-.76,.76]){const post=mesh(c,b,'front-post',new THREE.BoxGeometry(.08,1.27,.08),timber,'building');post.position.set(x,.72,.6);}for(const x of[-.48,.48]){const door=addPivot(c,b,x<0?'left-door':'right-door',[x<0?0:0,.66,.64]);const panel=mesh(c,door,'double-door-panel',new THREE.BoxGeometry(.92,1.12,.06),timber,'doors');panel.position.x=x<0?-.46:.46;door.rotation.y=x<0?.08:-.08;addChannel(c,door,'rotation','y',x<0?-.62:.62,.8,0);const handle=mesh(c,door,'iron-handle',new THREE.BoxGeometry(.06,.13,.07),iron,'doors');handle.position.set(x<0?-.16:.16,0,.06);const hinge=mesh(c,door,'iron-hinge',new THREE.BoxGeometry(.07,.11,.07),iron,'doors');hinge.position.set(x<0?-.82:.82,.22,.06);}for(const y of[.45,.78]){const sh=mesh(c,b,'interior-shelf',new THREE.BoxGeometry(1.45,.06,.34),shelf,'interior');sh.position.set(0,y,-.18);}const vent=mesh(c,b,'side-vent',new THREE.BoxGeometry(.08,.28,.36),iron,'service');vent.position.set(1.05,.87,.12);const rack=addPivot(c,b,'covered-side-rack',[1.35,.3,-.22]);mesh(c,rack,'side-rack-shelf',new THREE.BoxGeometry(.5,.08,.45),shelf,'rack');const rackRoof=mesh(c,rack,'side-rack-cover',new THREE.BoxGeometry(.62,.06,.55),roof,'rack');rackRoof.position.y=.5;for(const x of[-.22,.22]){const leg=mesh(c,rack,'side-rack-leg',new THREE.BoxGeometry(.05,.52,.05),iron,'rack');leg.position.set(x,.25,0);}addSocket(c,b,'terrain-anchor',[0,0,0]);addSocket(c,b,'loading',[0,.1,.82]);addSocket(c,b,'service',[1.22,.35,.05]);addSocket(c,b,'attachment',[0,1.3,-.7]);addSocket(c,b,'adjacency-east',[1.2,0,0]);addCollider(c,b,'building','box',[0,.72,0],{width:2.1,height:1.4,depth:1.25,isTrigger:false});addCollider(c,b,'doors','box',[0,.66,.64],{width:1.85,height:1.12,depth:.1,isTrigger:false});return finishAsset(c);}export function animateStorageShed(root,time){for(const ch of root?.userData?.sculptRuntime?.animationChannels??[])ch.node.rotation.y=ch.baseValue+Math.sin((Number.isFinite(time)?time:0)*ch.frequency+ch.phase)*ch.amplitude;return root;}
