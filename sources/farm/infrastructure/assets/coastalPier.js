import * as THREE from 'three';import {addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';import {faceted} from '../core/geometryLibrary.js';const mat=(n,c,r=.7,m=0)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:r,metalness:m,flatShading:true,vertexColors:true});const M=(c,p,i,g,x,d)=>registerMesh(c,p,i,new THREE.Mesh(faceted(g,`${c.id}:${i}`),x),d,'optimization-pass');const B=(c,p,i,s,o,x,d)=>{const q=M(c,p,i,new THREE.BoxGeometry(...s),x,d);q.position.set(...o);return q};const beam=(c,p,i,a,b,w,x,d)=>{const A=new THREE.Vector3(...a),D=new THREE.Vector3(...b),q=B(c,p,i,[w,A.distanceTo(D),w],A.clone().add(D).multiplyScalar(.5).toArray(),x,d);q.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),D.sub(A).normalize());return q};
export function createCoastalPier(){const c=createAssetContext('coastal-pier',{label:'Coastal Pier',targetHeightMetres:2.5,passId:'optimization-pass'}),{model}=c,x={teal:mat('pier-muted-teal','#4c7473',.62,.1),iron:mat('pier-dark-metal','#30393a',.5,.3),timber:mat('pier-salt-timber','#806b51',.86),cream:mat('pier-warm-cream','#d0c4ac',.78),galv:mat('pier-galvanized','#8b9895',.46,.27)};const pier=addPivot(c,model,'fixed-pier'),end=addPivot(c,pier,'widened-end-platform'),canopy=addPivot(c,end,'service-canopy');B(c,pier,'shore-approach-deck',[2.1,.18,6.2],[0,1.15,0],x.timber,'deck');B(c,end,'wide-end-deck',[3.5,.18,2.2],[0,1.15,-3.4],x.timber,'deck');for(const z of [-2.5,-1.2,0,1.2,2.5])for(const dx of [-.78,.78])beam(c,pier,`saltwater-pile-${dx}-${z}`,[dx,0,z],[dx,1.15,z],.2,x.iron,'piles');for(const dx of [-1.35,1.35])for(const z of [-4.1,-3.4,-2.7])beam(c,end,`end-pile-${dx}-${z}`,[dx,0,z],[dx,1.15,z],.2,x.iron,'piles');for(const dx of [-.9,.9])for(const z of [-2.2,-.9,.4,1.7,3])beam(c,pier,`rail-post-${dx}-${z}`,[dx,1.22,z],[dx,1.85,z],.07,x.galv,'rails');for(const dx of [-.9,.9])beam(c,pier,`rail-top-${dx}`,[dx,1.85,-2.2],[dx,1.85,3],.07,x.galv,'rails');for(const dx of [-1.05,1.05]){const cleat=M(c,end,`mooring-cleat-${dx}`,new THREE.CylinderGeometry(.11,.11,.32,8),x.cream,'mooring');cleat.position.set(dx,1.34,-3.85);cleat.rotation.z=Math.PI/2;}B(c,canopy,'small-service-canopy',[1.35,.14,1.05],[0,2.35,-3.35],x.teal,'canopy');for(const dx of [-.52,.52])beam(c,canopy,`canopy-post-${dx}`,[dx,1.25,-3.7],[dx,2.35,-3.7],.08,x.galv,'canopy');for(const [i,o] of [['ground',[0,0,0]],['shore',[0,1.15,3.2]],['water-left',[-1.6,1.15,-3.4]],['water-right',[1.6,1.15,-3.4]],['boat-mooring',[0,1.2,-4.6]],['service',[0,1.3,-3.2]]])addSocket(c,model,i,o);addCollider(c,pier,'pier-deck','box',[0,1.15,0],{width:2.1,height:.2,depth:6.2,isTrigger:false});addCollider(c,end,'end-platform','box',[0,1.15,-3.4],{width:3.5,height:.2,depth:2.2,isTrigger:false});addCollider(c,canopy,'service-zone','box',[0,1.8,-3.5],{width:1.4,height:1.2,depth:1.1,isTrigger:true});const r=finishAsset(c);r.userData.artDirection={concept:'references/concepts/coastal-pier.png',identity:['long saltwater timber pier','widened end platform','rail breaks','mooring cleats','small service canopy'],motion:'Static pier.'};r.userData.qualityContract={triangleBudget:5000,optimizedDrawRange:[4,6],criticalFeatures:['long timber deck','piles','end platform','cleats','service canopy']};return r;}
