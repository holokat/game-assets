import * as THREE from 'three';
import {addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh} from '../core/assetContext.js';
import {faceted} from '../core/geometryLibrary.js';
const material=(name,color,roughness=.62)=>new THREE.MeshStandardMaterial({name,color,roughness,flatShading:true,vertexColors:true});
const mesh=(c,p,id,g,m,group)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),m),group);
export function createAnimalWashStation(){
 const c=createAssetContext('animal-wash-station'),{model}=c,padMat=material('animal-wash-station-non-slip-pad','#9b988d'),iron=material('animal-wash-station-dark-rail','#354145',.42),water=material('animal-wash-station-drainage-blue-gray','#6595a0',.5),cabinet=material('animal-wash-station-pump-cabinet','#8c7b61'),gateMat=material('animal-wash-station-gate','#5e513e');
 addSocket(c,model,'ground',[0,0,0]);const station=addPivot(c,model,'open-animal-wash-station',[0,0,0]);const pad=mesh(c,station,'open-non-slip-wash-pad',new THREE.BoxGeometry(3.6,.12,2.15),padMat,'pad');pad.position.y=.06;const drain=mesh(c,station,'shallow-drainage-channel',new THREE.BoxGeometry(3.15,.035,.22),water,'drainage');drain.position.set(0,.14,-.78);const grate=mesh(c,station,'drainage-grate',new THREE.BoxGeometry(2.3,.025,.04),iron,'drainage');grate.position.set(0,.17,-.78);
 for(const z of[-.72,.72]){const rail=mesh(c,station,'slim-side-wash-rail',new THREE.BoxGeometry(2.75,.07,.07),iron,'rails');rail.position.set(.05,.7,z);for(const x of[-1.3,1.4]){const post=mesh(c,station,'slim-side-rail-post',new THREE.BoxGeometry(.08,.85,.08),iron,'rails');post.position.set(x,.48,z);}}
 for(const x of[-.52,.52]){const archPost=mesh(c,station,'overhead-spray-arch-post',new THREE.CylinderGeometry(.06,.06,1.35,6),iron,'spray');archPost.position.set(x,.82,.03);}const arch=mesh(c,station,'restrained-overhead-spray-pipe-arch',new THREE.CylinderGeometry(.06,.06,1.15,6),iron,'spray');arch.position.set(0,1.48,.03);arch.rotation.z=Math.PI/2;const manifold=mesh(c,station,'hose-manifold-cue',new THREE.BoxGeometry(.42,.12,.12),iron,'spray');manifold.position.set(.52,1.15,.03);
 const gate=addPivot(c,station,'broad-wash-entry-gate',[-1.8,.5,1.07]);const gateFrame=mesh(c,gate,'broad-wash-entry-gate-frame',new THREE.BoxGeometry(1.42,.7,.07),gateMat,'gate');gateFrame.position.set(.71,0,0);addChannel(c,gate,'rotation','y',.72,.65,0);
 const pump=mesh(c,station,'small-pump-control-cabinet',new THREE.BoxGeometry(.5,.62,.45),cabinet,'service');pump.position.set(1.43,.37,-.55);const power=mesh(c,station,'power-control-box',new THREE.BoxGeometry(.16,.26,.12),iron,'service');power.position.set(1.73,.56,-.55);
 addSocket(c,station,'terrain-anchor',[0,0,0]);addSocket(c,station,'livestock-entry',[-1.75,.25,1.15]);addSocket(c,station,'livestock-exit',[1.85,.25,1.08]);addSocket(c,station,'wash-bay',[0,.25,0]);addSocket(c,station,'water-in',[1.72,.5,-.55]);addSocket(c,station,'drain-out',[0,.15,-1.05]);addSocket(c,station,'power',[1.85,.56,-.55]);addSocket(c,station,'service',[1.45,.37,-.55]);addSocket(c,station,'attachment',[0,1.55,.03]);addSocket(c,station,'adjacency-east',[1.95,0,0]);addSocket(c,station,'ground',[0,0,0]);
 addCollider(c,station,'wash-pad','box',[0,.06,0],{width:3.6,height:.12,depth:2.15,isTrigger:true});addCollider(c,station,'entry-gate','box',[-1.09,.5,1.07],{width:1.42,height:.7,depth:.1,isTrigger:false});addCollider(c,station,'pump-cabinet','box',[1.43,.37,-.55],{width:.5,height:.62,depth:.45,isTrigger:false});addCollider(c,station,'spray-arch','box',[0,.82,.03],{width:1.15,height:1.4,depth:.15,isTrigger:false});return finishAsset(c);
}
export function animateAnimalWashStation(root,time){for(const ch of root?.userData?.sculptRuntime?.animationChannels||[])ch.node.rotation.y=ch.baseValue+Math.sin((Number.isFinite(time)?time:0)*ch.frequency+ch.phase)*ch.amplitude;return root;}
