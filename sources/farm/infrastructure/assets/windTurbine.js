import*as THREE from'three';import{addChannel,addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';const M=(n,c)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:.75,flatShading:true}),m=(c,p,id,g,x,gr)=>registerMesh(c,p,id,new THREE.Mesh(g,x),gr,'structural-pass');export function createWindTurbine(){const c=createAssetContext('wind-turbine',{label:'Wind Turbine'});c.materials={tower:M('wind-turbine-white','#d4d5d0'),base:M('wind-turbine-base','#7d7d78'),dark:M('wind-turbine-nacelle','#60676a')};const{model}=c;addSocket(c,model,'ground',[0,0,0]);addSocket(c,model,'power-output',[.4,.2,0]);addSocket(c,model,'wind-axis',[0,4.45,0]);const tower=addPivot(c,model,'tapered-tower');const base=m(c,tower,'concrete-base',new THREE.CylinderGeometry(.48,.64,.28,8),c.materials.base,'base');base.position.y=.14;const mast=m(c,tower,'mast',new THREE.CylinderGeometry(.18,.35,4.0,6),c.materials.tower,'tower');mast.position.y=2.25;addCollider(c,tower,'tower','cylinder',[0,2.2,0],{radius:.64,height:4.4,isTrigger:false});const nacelle=addPivot(c,model,'nacelle',[0,4.3,0]);const n=m(c,nacelle,'generator-body',new THREE.BoxGeometry(.75,.42,.55),c.materials.dark,'nacelle');n.position.x=.25;const rotor=addPivot(c,nacelle,'rotor',[-.22,0,0]);m(c,rotor,'hub',new THREE.CylinderGeometry(.22,.22,.25,8),c.materials.tower,'rotor').rotation.x=Math.PI/2;for(let i=0;i<3;i++){const b=m(c,rotor,`blade-${i}`,new THREE.BoxGeometry(.22,1.65,.08),c.materials.tower,'rotor');b.position.y=.94;b.rotation.z=i*Math.PI*2/3;}addCollider(c,rotor,'rotor','sphere',[0,0,0],{radius:1.8,isTrigger:true});addChannel(c,rotor,'rotation','x',Math.PI*2,0.22);return finishAsset(c)}
