import * as THREE from 'three';import{addCollider,addPivot,addSocket,createAssetContext,finishAsset,registerMesh}from'../core/assetContext.js';import{faceted}from'../core/geometryLibrary.js';const m=(n,c)=>new THREE.MeshStandardMaterial({name:n,color:c,roughness:.75,flatShading:true,vertexColors:true});const q=(c,p,id,g,x,z)=>registerMesh(c,p,id,new THREE.Mesh(faceted(g,`${c.id}:${id}`),x),z);export function createRicePaddy(){const c=createAssetContext('rice-paddy'),water=m('rice-paddy-water-mat','#547f91'),earth=m('rice-paddy-earth-mat','#81613d'),shoot=m('rice-paddy-shoot-mat','#608147'),gate=m('rice-paddy-gate-mat','#456f69'),{model}=c;addSocket(c,model,'ground',[0,0,0]);const b=addPivot(c,model,'paddy-bed',[0,.0175,0]);q(c,b,'water-plane',new THREE.BoxGeometry(2.4,.035,1.5),water,'water');for(let r=0;r<4;r++)for(let x=-1;x<=1;x+=.25){const s=q(c,b,'rice-shoot',new THREE.ConeGeometry(.028,.12,4),shoot,'plants');s.position.set(x,.115,-.45+r*.3);}for(const[x,z,w,d]of[[0,-.78,2.55,.08],[0,.78,2.55,.08],[-1.28,0,.08,1.6],[1.28,0,.08,1.6]]){const e=q(c,b,'earth-berm',new THREE.BoxGeometry(w,.13,d),earth,'berm');e.position.set(x,.09,z);}for(const[x,id]of[[-1.34,'flow-in'],[1.34,'flow-out']]){const n=addPivot(c,b,id,[x,.1,0]);q(c,n,id,new THREE.BoxGeometry(.12,.12,.25),gate,'flow');addSocket(c,n,id,[x,0,0]);}addSocket(c,b,'terrain-anchor',[0,0,0]);addSocket(c,b,'adjacency-east',[1.3,0,0]);addCollider(c,b,'paddy','box',[0,0,0],{width:2.6,height:.25,depth:1.6,isTrigger:false});return finishAsset(c);}
