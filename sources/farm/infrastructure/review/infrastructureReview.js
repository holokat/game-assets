import * as THREE from 'three'; import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
export function mountInfrastructureReview({create,label,id,span}){const host=document.querySelector('#canvas-host'),stats=document.querySelector('#stats'),renderer=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.shadowMap.enabled=true;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;host.append(renderer.domElement);const scene=new THREE.Scene();scene.background=new THREE.Color(0xf6eee3);const camera=new THREE.PerspectiveCamera(32,1,.01,100);camera.position.set(10.5,6.4,12);const controls=new OrbitControls(camera,renderer.domElement);controls.target.set(0,1,0);controls.enableDamping=true;controls.enablePan=false;scene.add(new THREE.HemisphereLight(0xfff9ef,0x766c60,1.9));const key=new THREE.DirectionalLight(0xffddb8,3);key.position.set(-3.5,5.4,4.8);key.castShadow=true;key.shadow.mapSize.set(2048,2048);scene.add(key);const fill=new THREE.DirectionalLight(0xdce4e1,.9);fill.position.set(4,2,3);scene.add(fill);const ground=new THREE.Mesh(new THREE.CircleGeometry(5,64),new THREE.ShadowMaterial({color:0x4a4036,opacity:.12,transparent:true}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);const asset=create();scene.add(asset);let meshes=0,triangles=0;asset.traverse(n=>{if(n.isMesh){meshes++;triangles+=(n.geometry.index?.count??n.geometry.getAttribute('position')?.count??0)/3;}});const resize=()=>{renderer.setSize(host.clientWidth,host.clientHeight,false);camera.aspect=host.clientWidth/host.clientHeight;camera.updateProjectionMatrix();};new ResizeObserver(resize).observe(host);resize();const render=()=>{controls.update();renderer.render(scene,camera);requestAnimationFrame(render);};render();stats.textContent=`${meshes} visible meshes · ${Math.round(triangles).toLocaleString()} triangles · ${span} m span`;window[`__${id.toUpperCase().replaceAll('-','_')}_REVIEW_READY__`]=true;window[`__${id.toUpperCase().replaceAll('-','_')}_REVIEW_STATE__`]={stats:{meshes,triangles:Math.round(triangles)},runtime:asset.userData.sculptRuntime};}
