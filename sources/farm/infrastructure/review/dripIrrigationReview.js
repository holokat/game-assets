import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { DRIP_IRRIGATION_PASSES, animateDripIrrigation, createDripIrrigation } from '../assets/dripIrrigation.js';
const query = new URLSearchParams(window.location.search); const passId = DRIP_IRRIGATION_PASSES.includes(query.get('pass')) ? query.get('pass') : 'optimization-pass'; const animate = query.get('animate') !== '0'; const canvasHost = document.querySelector('#canvas-host'); const select = document.querySelector('#pass-select'); const statsElement = document.querySelector('#stats'); select.value = passId; select.addEventListener('change', () => { query.set('pass', select.value); window.location.search = query.toString(); });
const renderer = new THREE.WebGLRenderer({ antialias: true, powerPreference: 'high-performance', preserveDrawingBuffer: true }); renderer.setPixelRatio(Math.min(window.devicePixelRatio || 1, 2)); renderer.shadowMap.enabled = true; renderer.shadowMap.type = THREE.PCFShadowMap; renderer.outputColorSpace = THREE.SRGBColorSpace; renderer.toneMapping = THREE.ACESFilmicToneMapping; renderer.toneMappingExposure = 1.04; canvasHost.appendChild(renderer.domElement);
const scene = new THREE.Scene(); scene.background = new THREE.Color(0xf6eee3); const camera = new THREE.PerspectiveCamera(31, 1, 0.01, 100); camera.position.set(5.8, 4.6, 5.8); const controls = new OrbitControls(camera, renderer.domElement); controls.target.set(0, 0.58, 0); controls.enableDamping = true; controls.enablePan = false; controls.minDistance = 4; controls.maxDistance = 10; scene.add(new THREE.HemisphereLight(0xfff9ef, 0x738185, 1.9)); const key = new THREE.DirectionalLight(0xffe4c6, 2.8); key.position.set(-4.5, 6, 4.5); key.castShadow = true; key.shadow.mapSize.set(2048, 2048); key.shadow.camera.left = -4; key.shadow.camera.right = 4; key.shadow.camera.top = 4; key.shadow.camera.bottom = -3; scene.add(key); const fill = new THREE.DirectionalLight(0xddebea, 1.15); fill.position.set(4, 3, 4); scene.add(fill); const rim = new THREE.DirectionalLight(0xfff3db, passId === 'lighting-pass' ? 1.1 : 0.68); rim.position.set(1.5, 4.2, -4); scene.add(rim); const ground = new THREE.Mesh(new THREE.CircleGeometry(4, 48), new THREE.ShadowMaterial({ color: 0x4a4036, opacity: 0.11, transparent: true })); ground.rotation.x = -Math.PI / 2; ground.receiveShadow = true; scene.add(ground); const asset = createDripIrrigation({ passId }); scene.add(asset);
function countVisible(root) { let meshes = 0; let triangles = 0; root.traverse((node) => { if (node.visible && node.isMesh) { meshes += 1; triangles += (node.geometry.index?.count ?? node.geometry.getAttribute('position')?.count ?? 0) / 3; } }); return { meshes, triangles: Math.round(triangles) }; } function resize() { const w = canvasHost.clientWidth; const h = canvasHost.clientHeight; renderer.setSize(w, h, false); camera.aspect = w / Math.max(h, 1); camera.updateProjectionMatrix(); } resize(); new ResizeObserver(resize).observe(canvasHost); const start = performance.now(); function frame(now) { if (animate && passId === 'interaction-pass') animateDripIrrigation(asset, (now - start) / 1000, 0.9); controls.update(); renderer.render(scene, camera); requestAnimationFrame(frame); } requestAnimationFrame(frame); const stats = countVisible(asset); statsElement.textContent = `${passId} · ${stats.meshes} meshes · ${stats.triangles.toLocaleString()} triangles · 1.55 m art-direction height`; window.__DRIP_IRRIGATION_REVIEW_READY__ = true; window.__DRIP_IRRIGATION_REVIEW_STATE__ = { passId, stats, animate, rig: asset.userData.dripIrrigationRig, runtime: asset.userData.sculptRuntime };
