import * as THREE from 'three'; import { OrbitControls } from 'three/addons/controls/OrbitControls.js'; import { createVineyard } from '../assets/vineyard.js';
const host = document.querySelector('#canvas-host'), stats = document.querySelector('#stats'), renderer = new THREE.WebGLRenderer({ antialias: true, preserveDrawingBuffer: true }); renderer.setPixelRatio(Math.min(devicePixelRatio, 2)); renderer.shadowMap.enabled = true; renderer.outputColorSpace = THREE.SRGBColorSpace; renderer.toneMapping = THREE.ACESFilmicToneMapping; host.appendChild(renderer.domElement);
const scene = new THREE.Scene(); scene.background = new THREE.Color(0xf5ede1); const camera = new THREE.PerspectiveCamera(30, 1, 0.01, 100); camera.position.set(7.2, 6.2, 8.2); const controls = new OrbitControls(camera, renderer.domElement); controls.target.set(0, 0.55, 0); controls.enableDamping = true; controls.enablePan = false;
scene.add(new THREE.HemisphereLight(0xfff7eb, 0x756959, 2)); const key = new THREE.DirectionalLight(0xffd8aa, 3); key.position.set(-3, 5, 4); key.castShadow = true; key.shadow.mapSize.set(2048, 2048); scene.add(key); const fill = new THREE.DirectionalLight(0xd9e4df, 0.8); fill.position.set(3, 2, -2); scene.add(fill); const floor = new THREE.Mesh(new THREE.CircleGeometry(5, 64), new THREE.ShadowMaterial({ color: 0x493c32, opacity: 0.13, transparent: true })); floor.rotation.x = -Math.PI / 2; floor.receiveShadow = true; scene.add(floor);
const vineyard = createVineyard(); scene.add(vineyard); let meshes = 0, triangles = 0; vineyard.traverse((node) => { if (node.isMesh || node.isInstancedMesh) { meshes += 1; triangles += ((node.geometry.index?.count ?? node.geometry.getAttribute('position')?.count ?? 0) / 3) * (node.isInstancedMesh ? node.count : 1); } }); function resize() { renderer.setSize(host.clientWidth, host.clientHeight, false); camera.aspect = host.clientWidth / host.clientHeight; camera.updateProjectionMatrix(); } resize(); new ResizeObserver(resize).observe(host); function render() { controls.update(); renderer.render(scene, camera); requestAnimationFrame(render); } render(); stats.textContent = `${meshes} visible meshes · ${Math.round(triangles)} triangles · 3 trellis rows`; window.__VINEYARD_REVIEW_READY__ = true; window.__VINEYARD_REVIEW_STATE__ = { meshes, triangles };
