import * as THREE from 'three';import{OrbitControls}from'three/addons/controls/OrbitControls.js';import{createGlasshouse}from'../assets/glasshouse.js';const h=document.querySelector('#canvas-host'),s=document.querySelector('#stats'),r=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});r.setPixelRatio(Math.min(devicePixelRatio,2));r.shadowMap.enabled=true;r.outputColorSpace=THREE.SRGBColorSpace;r.toneMapping=THREE.ACESFilmicToneMapping;h.appendChild(r.domElement);const scene=new THREE.Scene();scene.background=new THREE.Color(0xf5ede1);const cam=new THREE.PerspectiveCamera(29,1,.01,100);cam.position.set(7.7,4.8,8.1);const ctrl=new OrbitControls(cam,r.domElement);ctrl.target.set(0,1,0);ctrl.enableDamping=true;ctrl.enablePan=false;scene.add(new THREE.HemisphereLight(0xfff7eb,0x718083,2));const key=new THREE.DirectionalLight(0xffd8aa,3);key.position.set(-3,5,4);key.castShadow=true;scene.add(key);const floor=new THREE.Mesh(new THREE.CircleGeometry(5,64),new THREE.ShadowMaterial({color:0x493c32,opacity:.13,transparent:true}));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;scene.add(floor);const house=createGlasshouse();scene.add(house);let meshes=0,tris=0;house.traverse(n=>{if(n.isMesh){meshes++;tris+=(n.geometry.index?.count??n.geometry.getAttribute('position')?.count??0)/3}});function z(){r.setSize(h.clientWidth,h.clientHeight,false);cam.aspect=h.clientWidth/h.clientHeight;cam.updateProjectionMatrix()}z();new ResizeObserver(z).observe(h);function loop(t){for(const ch of house.userData.sculptRuntime.animationChannels)ch.node[ch.property][ch.axis]=ch.baseValue+ch.amplitude*Math.sin(t*.001*ch.frequency+ch.phase);ctrl.update();r.render(scene,cam);requestAnimationFrame(loop)}loop(0);s.textContent=`${meshes} visible meshes · ${Math.round(tris)} triangles · deterministic double doors`;window.__GLASSHOUSE_REVIEW_READY__=true;window.__GLASSHOUSE_REVIEW_STATE__={meshes,tris,channels:house.userData.sculptRuntime.animationChannels.length};
