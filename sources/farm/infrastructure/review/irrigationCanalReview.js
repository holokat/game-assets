import * as THREE from 'three'; import { OrbitControls } from 'three/addons/controls/OrbitControls.js'; import { animateIrrigationCanal, createIrrigationCanal } from '../assets/irrigationCanal.js';
const canvasHost=document.querySelector('#canvas-host'); const statsElement=document.querySelector('#stats'); const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance',preserveDrawingBuffer:true}); renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2)); renderer.shadowMap.enabled=true; renderer.shadowMap.type=THREE.PCFShadowMap; renderer.outputColorSpace=THREE.SRGBColorSpace; renderer.toneMapping=THREE.ACESFilmicToneMapping; renderer.toneMappingExposure=1.02; canvasHost.appendChild(renderer.domElement); const scene=new THREE.Scene(); scene.background=new THREE.Color(0xf6eee3); const camera=new THREE.PerspectiveCamera(35,1,.01,100); camera.position.set(3.7,2.8,5.1); const controls=new OrbitControls(camera,renderer.domElement); controls.target.set(0,.45,0); controls.enableDamping=true; controls.enablePan=false; controls.minDistance=3; controls.maxDistance=9; scene.add(new THREE.HemisphereLight(0xfff9ef,0x7c7162,1.9)); const key=new THREE.DirectionalLight(0xffddb8,3); key.position.set(-3.5,5.4,4.8); key.castShadow=true; key.shadow.mapSize.set(2048,2048); scene.add(key); const fill=new THREE.DirectionalLight(0xdce4e1,1); fill.position.set(4,2.4,4); scene.add(fill); const ground=new THREE.Mesh(new THREE.CircleGeometry(4.2,64),new THREE.ShadowMaterial({color:0x4a4036,opacity:.11,transparent:true})); ground.rotation.x=-Math.PI/2; ground.receiveShadow=true; scene.add(ground); const canal=createIrrigationCanal(); scene.add(canal); function stats(root){let meshes=0,triangles=0;root.traverse(n=>{if(!n.visible||!n.isMesh)return;meshes++;triangles+=(n.geometry.index?.count??n.geometry.getAttribute('position')?.count??0)/3});return{meshes,triangles:Math.round(triangles)}}function resize(){const w=canvasHost.clientWidth,h=canvasHost.clientHeight;renderer.setSize(w,h,false);camera.aspect=w/Math.max(h,1);camera.updateProjectionMatrix()}resize();new ResizeObserver(resize).observe(canvasHost);const start=performance.now();function frame(now){animateIrrigationCanal(canal,(now-start)/1000,.42);controls.update();renderer.render(scene,camera);requestAnimationFrame(frame)}requestAnimationFrame(frame);const result=stats(canal);statsElement.textContent=`${result.meshes} visible meshes · ${result.triangles.toLocaleString()} triangles · 3.8 m tileable length`;window.__IRRIGATION_CANAL_REVIEW_READY__=true;window.__IRRIGATION_CANAL_REVIEW_STATE__={stats:result,runtime:canal.userData.sculptRuntime,rig:canal.userData.irrigationCanalRig};
