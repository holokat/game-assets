import * as THREE from 'three';
const put=async(path,blob)=>{const r=await fetch(`/__artifact__?path=${encodeURIComponent(path)}`,{method:'POST',headers:{'Content-Type':'image/png'},body:blob});if(!r.ok)throw Error(await r.text())};
export async function captureAutonomyConcept({id,create,target=[0,1,0],distance=5}){const r=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});r.setSize(1200,1200);r.setScissorTest(true);r.shadowMap.enabled=true;r.outputColorSpace=THREE.SRGBColorSpace;r.toneMapping=THREE.ACESFilmicToneMapping;r.toneMappingExposure=1.05;const s=new THREE.Scene();s.background=new THREE.Color(0xf7f0e6);s.add(new THREE.HemisphereLight(0xfff8ef,0x728080,2));const key=new THREE.DirectionalLight(0xffe0bf,3);key.position.set(-4,6,5);key.castShadow=true;s.add(key);const fill=new THREE.DirectionalLight(0xddeceb,1.15);fill.position.set(4,3,3);s.add(fill);const floor=new THREE.Mesh(new THREE.CircleGeometry(4.8,64),new THREE.ShadowMaterial({opacity:.13,transparent:true}));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;s.add(floor);const asset=create();s.add(asset);const cameras=[[.78,.54,1],[1,.42,-.7],[-.95,.52,-.65],[0,1.15,.48]].map(([a,b,c])=>[a*distance,b*distance,c*distance]);for(let i=0;i<4;i++){const cam=new THREE.PerspectiveCamera(38,1,.01,100);cam.position.set(...cameras[i]);cam.lookAt(...target);const x=(i%2)*600,y=i<2?600:0;r.setViewport(x,y,600,600);r.setScissor(x,y,600,600);r.render(s,cam)}r.setScissorTest(false);await new Promise(resolve=>r.domElement.toBlob(async b=>{await put(`review/${id}/concept.png`,b);resolve()},'image/png'));r.dispose();asset.userData.sculptRuntime.dispose();window.__AUTONOMY_CONCEPT_READY__=true}
