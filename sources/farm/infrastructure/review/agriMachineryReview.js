import * as THREE from 'three';import {OrbitControls} from 'three/addons/controls/OrbitControls.js';
export function startAgriReview(create,id){const host=document.querySelector('#canvas-host'),r=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});r.setPixelRatio(1);r.shadowMap.enabled=true;host.appendChild(r.domElement);const s=new THREE.Scene(),cam=new THREE.PerspectiveCamera(34,1,.1,100);cam.position.set(4.8,3.4,6.4);const ctl=new OrbitControls(cam,r.domElement);ctl.target.set(0,.65,0);ctl.enableDamping=true;s.background=new THREE.Color(0xf7f0e6);s.add(new THREE.HemisphereLight(0xfff8ef,0x718080,2));const key=new THREE.DirectionalLight(0xffe0bf,3);key.position.set(-4,6,5);key.castShadow=true;s.add(key);const floor=new THREE.Mesh(new THREE.CircleGeometry(4.4,48),new THREE.ShadowMaterial({opacity:.14}));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;s.add(floor);s.add(create());const resize=()=>{r.setSize(host.clientWidth,host.clientHeight,false);cam.aspect=host.clientWidth/host.clientHeight;cam.updateProjectionMatrix()};new ResizeObserver(resize).observe(host);resize();let done=false;function frame(){ctl.update();r.render(s,cam);if(!done){done=true;r.domElement.toBlob(async b=>{await fetch(`/__artifact__?path=${encodeURIComponent(`review/${id}/browser-review.png`)}`,{method:'POST',headers:{'Content-Type':'image/png'},body:b});window.__AGRI_REVIEW_CAPTURED__=true})}requestAnimationFrame(frame)}frame();window.__AGRI_REVIEW_READY__=true}
