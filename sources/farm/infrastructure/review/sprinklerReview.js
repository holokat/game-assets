import * as THREE from 'three';import{OrbitControls}from'three/addons/controls/OrbitControls.js';import{animateSprinkler,createSprinkler}from'../assets/sprinkler.js';const h=document.querySelector('#canvas-host'),s=document.querySelector('#stats'),r=new THREE.WebGLRenderer({antialias:true,preserveDrawingBuffer:true});r.setPixelRatio(Math.min(devicePixelRatio,2));r.shadowMap.enabled=true;r.shadowMap.type=THREE.PCFShadowMap;r.outputColorSpace=THREE.SRGBColorSpace;r.toneMapping=THREE.ACESFilmicToneMapping;r.toneMappingExposure=1.02;h.appendChild(r.domElement);const scene=new THREE.Scene();scene.background=new THREE.Color(0xf6eee3);const camera=new THREE.PerspectiveCamera(35,1,.01,100);camera.position.set(2.2,1.7,3.1);const c=new OrbitControls(camera,r.domElement);c.target.set(0,.62,0);c.enableDamping=true;c.enablePan=false;scene.add(new THREE.HemisphereLight(0xfff9ef,0x7c7162,1.9));const key=new THREE.DirectionalLight(0xffddb8,3);key.position.set(-3.5,5.4,4.8);key.castShadow=true;key.shadow.mapSize.set(2048,2048);scene.add(key);const fill=new THREE.DirectionalLight(0xdce4e1,1);fill.position.set(4,2.4,4);scene.add(fill);const g=new THREE.Mesh(new THREE.CircleGeometry(1.7,64),new THREE.ShadowMaterial({color:0x4a4036,opacity:.11,transparent:true}));g.rotation.x=-Math.PI/2;g.receiveShadow=true;scene.add(g);const sprinkler=createSprinkler();scene.add(sprinkler);function stats(){let m=0,t=0;sprinkler.traverse(n=>{if(n.isMesh){m++;t+=(n.geometry.index?.count??n.geometry.getAttribute('position')?.count??0)/3}});return{m,t:Math.round(t)}}function resize(){const w=h.clientWidth,hh=h.clientHeight;r.setSize(w,hh,false);camera.aspect=w/hh;camera.updateProjectionMatrix()}resize();new ResizeObserver(resize).observe(h);const start=performance.now();function f(now){animateSprinkler(sprinkler,(now-start)/1000,.45);c.update();r.render(scene,camera);requestAnimationFrame(f)}requestAnimationFrame(f);const q=stats();s.textContent=`${q.m} visible meshes · ${q.t.toLocaleString()} triangles · 1.32 m height`;window.__SPRINKLER_REVIEW_READY__=true;window.__SPRINKLER_REVIEW_STATE__={stats:q,runtime:sprinkler.userData.sculptRuntime,rig:sprinkler.userData.sprinklerRig};
