import * as THREE from 'three'; import { OrbitControls } from 'three/addons/controls/OrbitControls.js'; import { createThreshingFloor } from '../assets/threshingFloor.js';
const host = document.querySelector('#canvas-host'); const stats = document.querySelector('#stats'); const renderer = new THREE.WebGLRenderer({ antialias: true, preserveDrawingBuffer: true }); renderer.setPixelRatio(Math.min(devicePixelRatio || 1, 2)); renderer.shadowMap.enabled = true; renderer.shadowMap.type = THREE.PCFShadowMap; renderer.outputColorSpace = THREE.SRGBColorSpace; renderer.toneMapping = THREE.ACESFilmicToneMapping; renderer.toneMappingExposure = 1.08; host.appendChild(renderer.domElement); const scene = new THREE.Scene(); scene.background = new THREE.Color(0xf7f0e6); const camera = new THREE.PerspectiveCamera(32, 1, 0.01, 100); camera.position.set(6.8, 6.2, 8.1); const controls = new OrbitControls(camera, renderer.domElement); controls.target.set(0, 0.55, -0.15); controls.enableDamping = true; controls.enablePan = false; controls.minDistance = 6; controls.maxDistance = 14; scene.add(new THREE.HemisphereLight(0xfff9ef, 0x708080, 2)); const key = new THREE.DirectionalLight(0xffe0bf, 3); key.position.set(-4.2, 6.5, 5.2); key.castShadow = true; key.shadow.mapSize.set(2048, 2048); scene.add(key); const fill = new THREE.DirectionalLight(0xddeceb, 1.25); fill.position.set(4, 3.2, 3.5); scene.add(fill); const ground = new THREE.Mesh(new THREE.CircleGeometry(4.2, 64), new THREE.ShadowMaterial({ color: 0x4a4036, opacity: 0.11, transparent: true })); ground.rotation.x = -Math.PI / 2; ground.receiveShadow = true; scene.add(ground); const asset = createThreshingFloor(); scene.add(asset);
function resize() { renderer.setSize(host.clientWidth, host.clientHeight, false); camera.aspect = host.clientWidth / Math.max(1, host.clientHeight); camera.updateProjectionMatrix(); } resize(); new ResizeObserver(resize).observe(host); let captured = false; function frame(now) { controls.update(); renderer.render(scene, camera); if (!captured && now > 800) { captured = true; renderer.domElement.toBlob(async (blob) => { if (!blob) return; const response = await fetch(`/__artifact__?path=${encodeURIComponent('review/threshing-floor/browser-review.png')}`, { method: 'POST', headers: { 'Content-Type': 'image/png' }, body: blob }); window.__THRESHING_FLOOR_REVIEW_CAPTURED__ = response.ok; }, 'image/png'); } requestAnimationFrame(frame); } requestAnimationFrame(frame); let meshes = 0; let triangles = 0; asset.traverse((node) => { if (!node.visible || !node.isMesh) return; meshes += 1; triangles += (node.geometry.index?.count ?? node.geometry.getAttribute('position')?.count ?? 0) / 3; }); stats.textContent = `${meshes} visible meshes · ${Math.round(triangles).toLocaleString()} triangles · 5 material families`; window.__THRESHING_FLOOR_REVIEW_READY__ = true;
