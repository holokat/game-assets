import { createWagonDepot } from '../assets/wagonDepot.js'; import { exportInventoryAsset } from './inventory122Export.js'; export const exportWagonDepot = () => exportInventoryAsset({ id: 'wagon-depot', title: 'Wagon-Depot', create: createWagonDepot }); const output = document.querySelector('#output'); window.__WAGON_DEPOT_EXPORT_READY__ = false; try { window.__WAGON_DEPOT_EXPORT_MANIFEST__ = await exportWagonDepot(); output.textContent = JSON.stringify(window.__WAGON_DEPOT_EXPORT_MANIFEST__, null, 2); window.__WAGON_DEPOT_EXPORT_READY__ = true; } catch (error) { output.textContent = String(error.stack || error); window.__WAGON_DEPOT_EXPORT_ERROR__ = String(error.stack || error); }
